#version 330

in vec4 vCol;
in vec2 TexCoord;
in vec3 FragPos;

out vec4 colour;

struct Material
{
	float specularIntensity;
	float shininess;
};

uniform sampler2D theTexture;

uniform Material material;

uniform vec3 eyePosition;


void main()
{
	colour = texture(theTexture, TexCoord) * vec4(1.0f,1.0f,1.0f,1.0f);
}